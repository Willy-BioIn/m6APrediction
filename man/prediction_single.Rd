% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Single-Sample Prediction of m6A Modification Status}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{A pre-trained machine learning model, identical to the \code{ml_fit} parameter in prediction_multiple (must support probability output).}

\item{gc_content}{A numeric value(0-1), GC content of the single sample (e.g., 0.45 = 45\%).}

\item{RNA_type}{A character string, RNA type of the single sample. \strong{Allowed values only}: "mRNA", "lincRNA", "lncRNA", "pseudogene".}

\item{RNA_region}{A character string, RNA region type of the single sample. \strong{Allowed values only}: "CDS", "intron", "3'UTR", "5'UTR".}

\item{exon_length}{A numeric value, exon length of the single sample.}

\item{distance_to_junction}{A numeric value, distance of the single sample to the junction.}

\item{evolutionary_conservation}{A numeric value, evolutionary conservation score of the single sample.}

\item{DNA_5mer}{A character string, 5-mer DNA sequence of the single sample. \strong{Must be 5 characters long} (containing only "A", "T", "C", "G").}

\item{positive_threshold}{A numeric value defining the probability threshold for positive predictions. Defaults to 0.5 (same as prediction_multiple).}
}
\value{
A named character vector with 2 elements:
\itemize{
\item "predicted_m6A_prob": Character-formatted numeric, probability of being predicted as m6A-positive (convert to numeric with as.numeric()).
\item "predicted_m6A_status": Character, predicted status ("Positive" or "Negative").
}
}
\description{
Takes feature parameters for a single sample (e.g., GC content, RNA type) and calls the prediction_multiple function
to predict m6A status. Eliminates the need to manually construct a data frame and directly returns a named vector
containing "predicted probability" and "predicted status" for easy quick calls.
}
\examples{
rf_fit<-readRDS(system.file("extdata", "rf_fit.rds", package="m6APrediction"))
result<-prediction_single(
  ml_fit = rf_fit,
  gc_content = 0.6,
  RNA_type = "mRNA",
  RNA_region = "3'UTR",
  exon_length = 12,
  distance_to_junction = 5,
  evolutionary_conservation = 0.7,
  DNA_5mer = "ATCGT",
  positive_threshold = 0.5
)
print(result)
}
\seealso{
\code{\link{prediction_multiple}} (underlying batch prediction function called by this function)
}
