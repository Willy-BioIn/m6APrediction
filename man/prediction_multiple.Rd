% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_multiple}
\alias{prediction_multiple}
\title{Batch Prediction of m6A Modification Status}
\usage{
prediction_multiple(ml_fit, feature_df, positive_threshold = 0.5)
}
\arguments{
\item{ml_fit}{A pre-trained machine learning model (e.g. randomForest).
Support the \code{predict()} method with \code{type = "prob"} to output a probability matrix (containing a "Positive" column).}

\item{feature_df}{A data frame \strong{required to contain the following 7 columns} (order does not matter):
gc_content (numeric, GC content), RNA_type (character/factor, RNA type),
RNA_region (character/factor, RNA region), exon_length (numeric, exon length),
distance_to_junction (numeric, distance to junction), evolutionary_conservation (numeric, evolutionary conservation score),
DNA_5mer (character, 5-mer DNA sequence, containing only "A", "T", "C", "G").}

\item{positive_threshold}{A numeric value defining the probability threshold for positive ("Positive") predictions. Defaults to 0.5.}
}
\value{
A data frame with 2 additional columns appended to the input \code{feature_df}:
\itemize{
\item predicted_m6A_prob: Numeric, probability of being predicted as m6A-positive.
\item predicted_m6A_status: Factor, predicted status with levels: "Negative" and "Positive".
}
}
\description{
Uses a pre-trained machine learning model and a batch feature data frame to predict m6A modification probability
and classification status for each sample. Automatically standardizes factor levels for RNA type and RNA region,
processes 5-mer sequences via the dna_encoding function, and returns the complete data frame with prediction results.
}
\examples{
rf_fit<-readRDS(system.file("extdata", "rf_fit.rds", package="m6APrediction"))
example_data <- read.csv(system.file("extdata", "m6A_input_example.csv", package = "m6APrediction"))
pred_result <- prediction_multiple(ml_fit = rf_fit, feature_df = example_data)
head(pred_result, 5)

}
\seealso{
\code{\link{dna_encoding}} (dependency function for DNA_5mer processing),
\code{\link{prediction_single}} (single-sample prediction function)
}
